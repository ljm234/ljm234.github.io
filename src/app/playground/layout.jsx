export const metadata = {
  title: "Playground — Amoebanator Triage Demo",
  description:
    "Interactive clinical ML demo with risk estimate, feature contributions, and a model card.",
};

export default function PlaygroundLayout({ children }) {
  return <>{children}</>;
}
